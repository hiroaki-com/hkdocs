{
  // Docusaurus と IDE の型チェックで使用する設定。
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": "."
  },
  "include": [
    "docusaurus.config.ts",
    "sidebars.ts",
    "src/**/*.ts",
    "src/**/*.tsx"
  ],
  "exclude": [".docusaurus", "build"]
}
